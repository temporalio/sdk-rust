[package]
name = "temporal-wasm-interceptor-workflow"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
temporalio-workflow = { path = "../../../../workflow" }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "temporal:interceptor-workflow"

[workspace]
