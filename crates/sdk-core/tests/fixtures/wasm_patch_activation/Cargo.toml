[package]
name = "temporal-wasm-patch-activation-workflow"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
temporalio-workflow = { path = "../../../../workflow" }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "temporal:patch-activation-workflow"

[workspace]
